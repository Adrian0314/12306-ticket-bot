# 仓库内的 config.json 是脱敏示例（无真实姓名/行程）。
# 修改成自己的行程后，请勿提交到公开仓库：
#   1. 建议使用私有仓库；或
#   2. 先执行 git rm --cached config.json 再把它加回本文件（gitignore 对已跟踪文件无效）
# 注意：本文件只对"未跟踪"的文件生效，config.json 一旦入库修改即会被跟踪。

# 运行日志：可能含行程与订单信息
ticket_log_v4.txt

# Python
__pycache__/
*.pyc
